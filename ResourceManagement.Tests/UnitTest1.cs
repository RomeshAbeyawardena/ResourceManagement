namespace ResourceManagement.Tests;

public class Tests
{
    [SetUp]
    public void Setup()
    {
    }

    [Test]
    public void Test1()
    {
        Assert.Pass();
    }

    //Check in a resource with valid primary key (without
    //Check in a resource with valid resource id and user id
    //Check in a resource with invalid resource id
    //Check in a resource with invalid user id
    //Check in a resource with valid resource user access id
    //Check in a resource with invalid resource user access id
    //Check in a resource with missing parameters
}